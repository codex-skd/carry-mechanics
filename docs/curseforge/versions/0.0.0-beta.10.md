## v0.0.0-beta.10

**fix:** La prevención de salto no funcionaba al estar el mixin en `LivingEntity` en lugar de `ServerPlayer` (que sobrescribe el método).

### Cambios
- 🔧 Mixin movido de `LivingEntity.jumpFromGround()` a `ServerPlayer.jumpFromGround()`
- ✅ El jugador ya no puede saltar mientras carga algo

### Requisitos
- Minecraft 26.1.2
- NeoForge 26.1.2.78

### Pendiente
- Renderizado visual del objeto cargado (API SubmitNodeCollector)
