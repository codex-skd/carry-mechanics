<h2>v0.0.0-beta.31 — Debug: Restored RenderLayer + Mixin Diagnostic Logging</h2>

<h3>Changes</h3>
<ul>
<li><strong>Restored CarryingItemRenderLayer</strong> with <code>ICarryOnRenderState</code> infrastructure (AvatarExtractorMixin, PlayerRenderStateMixin, AvatarRendererMixin).</li>
<li><strong>Added diagnostic logging</strong> to both the mixin and the render layer to trace the rendering pipeline.</li>
<li>Both approaches (mixin + layer) are active simultaneously to determine which path is failing.</li>
</ul>

<h3>Debug Logs</h3>
<ul>
<li><code>[MIXIN] extractRenderState called</code> — confirms entity caching in LivingEntityRendererMixin.</li>
<li><code>[MIXIN] submit called</code> — confirms the mixin submit injection fires.</li>
<li><code>[MIXIN] isCarrying=</code> — shows whether carry data is available on client.</li>
<li><code>[RENDER] CarryData from state</code> — confirms the RenderLayer approach fires.</li>
<li>Check <code>latest.log</code> after testing and share the output for analysis.</li>
</ul>
