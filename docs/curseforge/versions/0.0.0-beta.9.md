## v0.0.0-beta.9

**feat:** Slowness mínima siempre aplicada (nivel 1 como mínimo), prevención de salto mediante mixin y hook de render preparado.

### Cambios
- 🔧 Slowness: nivel mínimo 1 (Slowness I) para cualquier objeto transportado
- 🔧 Prevención de salto: mixin en `jumpFromGround()` cancelado si el jugador carga algo
- 🔧 Mixin `AvatarRenderer.renderRightHand()` añadido con la firma correcta de esta versión (pendiente de implementar el renderizado real)

### Requisitos
- Minecraft 26.1.2
- NeoForge 26.1.2.78

### Pendiente
- Renderizado visual del objeto cargado (API SubmitNodeCollector)
